---
title: Database
description: Add PostgreSQL or MySQL with Drizzle in one command
---

Scaffold [Drizzle ORM](https://orm.drizzle.team) with PostgreSQL or MySQL into your project.

---

## Installation

<Tabs items={['npm', 'pnpm', 'bun']}>
  <Tab value="npm">

```bash
npx zuro-cli add database
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx zuro-cli add database
```

  </Tab>
  <Tab value="bun">

```bash
bun x zuro-cli add database
```

  </Tab>
</Tabs>

You can skip the dialect prompt: `add database-pg` or `add database-mysql`.

---

## Usage

**1. Define a schema:**

```ts title="src/db/schema/posts.ts"
import { pgTable, text, timestamp } from "drizzle-orm/pg-core";

export const posts = pgTable("posts", {
  id: text("id").primaryKey(),
  title: text("title").notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});
```

**2. Export it:**

```ts title="src/db/schema/index.ts"
export * from "./posts";
```

**3. Run migrations:**

```bash
npx drizzle-kit generate
npx drizzle-kit migrate
```

**4. Query in a route:**

```ts
import { db } from "../db";
import { posts } from "../db/schema";

router.get("/posts", async (req, res) => {
  const allPosts = await db.select().from(posts);
  res.json(allPosts);
});
```

---

## Configuration

Your `.env` gets a `DATABASE_URL` automatically:

```bash
# PostgreSQL
DATABASE_URL=postgresql://postgres@localhost:5432/mydb

# MySQL
DATABASE_URL=mysql://root@localhost:3306/mydb
```

| Provider    | URL Format                                                     |
| ----------- | -------------------------------------------------------------- |
| Neon        | `postgresql://user:pass@ep-xxx.neon.tech/mydb?sslmode=require` |
| PlanetScale | `mysql://user:pass@aws.connect.psdb.cloud/mydb?ssl=...`        |
| Supabase    | `postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres`  |

---

## Troubleshooting

- **Invalid database URL** — must start with `postgresql://` or `mysql://`
- **Migration errors** — check that your database is running and `DATABASE_URL` is correct
- **Switching dialects** — Zuro creates a backup in `.zuro/backups/` before replacing files

---

## Next Steps

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <a
    href="/docs/auth"
    className="block border border-border rounded-xl p-5 bg-card hover:border-green-500/50 transition-colors no-underline"
  >
    <h3 className="font-bold text-foreground mb-1">→ Add Auth</h3>
    <p className="text-sm text-muted-foreground">
      User signup, login, and sessions
    </p>
  </a>
  <a
    href="/docs/error-handler"
    className="block border border-border rounded-xl p-5 bg-card hover:border-green-500/50 transition-colors no-underline"
  >
    <h3 className="font-bold text-foreground mb-1">→ Add Error Handler</h3>
    <p className="text-sm text-muted-foreground">
      Consistent API error responses
    </p>
  </a>
</div>
