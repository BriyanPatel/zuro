---
title: Validator
description: Simple request validation with Zod
---

Validate `body`, `query`, and `params` with Zod schemas before your route handlers run.

---

## Installation

<Tabs items={['npm', 'pnpm', 'bun']}>
  <Tab value="npm">

```bash
npx zuro-cli add validator
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx zuro-cli add validator
```

  </Tab>
  <Tab value="bun">

```bash
bun x zuro-cli add validator
```

  </Tab>
</Tabs>

---

## Usage

**Validate request body:**

```ts
import { Router } from "express";
import { z } from "zod";
import { validate } from "../middleware/validate";

const router = Router();

const createUserSchema = z.object({
  body: z.object({
    name: z.string().min(2),
    email: z.string().email(),
  }),
});

router.post("/users", validate(createUserSchema), (req, res) => {
  const { name, email } = req.body;
  res.json({ name, email });
});
```

**Validate query and params:**

```ts
const listPostsSchema = z.object({
  query: z.object({
    page: z.coerce.number().min(1).default(1),
    limit: z.coerce.number().min(1).max(100).default(20),
  }),
  params: z.object({
    userId: z.string().uuid(),
  }),
});

router.get("/users/:userId/posts", validate(listPostsSchema), (req, res) => {
  const { page, limit } = req.query;
  const { userId } = req.params;
  res.json({ page, limit, userId });
});
```

If validation fails, the client gets a `422`:

```json
{
  "status": "error",
  "code": "VALIDATION_ERROR",
  "message": "Validation failed",
  "errors": [{ "path": "body.email", "message": "Invalid email" }]
}
```

---

## Configuration

No environment variables needed. The validator works with `zod` which is already included in core.

Parsed values are written back to `req` automatically — so Zod defaults and coercion just work.

---

## Troubleshooting

- **422 Validation failed** — check that your request matches the Zod schema fields and types
- **Query/params not validating** — wrap them in `z.object({ query: ..., params: ... })`
- **Non-Zod errors** — they pass through to your error handler middleware

---

## Next Steps

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <a
    href="/docs/error-handler"
    className="block border border-border rounded-xl p-5 bg-card hover:border-green-500/50 transition-colors no-underline"
  >
    <h3 className="font-bold text-foreground mb-1">→ Add Error Handler</h3>
    <p className="text-sm text-muted-foreground">
      Centralized API error responses
    </p>
  </a>
  <a
    href="/docs/auth"
    className="block border border-border rounded-xl p-5 bg-card hover:border-green-500/50 transition-colors no-underline"
  >
    <h3 className="font-bold text-foreground mb-1">→ Add Auth</h3>
    <p className="text-sm text-muted-foreground">
      User auth and protected routes
    </p>
  </a>
</div>
