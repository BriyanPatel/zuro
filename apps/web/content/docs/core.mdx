---
title: Core Module
description: The foundation of your project.
---

# Core Module

The Core module is installed automatically when you run `zuro-cli init`.

## What it sets up
1. **Strict TypeScript Config**: No more `any`.
2. **Environment Validation**: Uses Zod to ensure `.env` variables exist.
3. **Security**: Pre-configured `helmet` and `cors`.
